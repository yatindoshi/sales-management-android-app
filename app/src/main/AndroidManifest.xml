<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cgal.salesmanagement">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.SalesManagement">


        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">   <!-- 👈 Required -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false" />
        <activity android:name=".ui.salesman.ViewPendingRequestsActivity" />
        <activity android:name=".ui.reports.SalesReportActivity" />
        <activity android:name=".ui.stock.AddStockActivity" />
        <activity android:name=".ui.stock.ViewStockActivity" />
        <activity android:name=".ui.users.PendingRequestsActivity" />
        <activity
            android:name=".ui.users.PendingRequestDetailsActivity"
            android:exported="false"/>

    </application>

</manifest>
